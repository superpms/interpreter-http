# Commands

This package contributes one development command when terminal command support is installed.

## Read

- [Dev HTTP Server](dev-http-server.md)

## Boundary

The command is a convenience wrapper around PHP's built-in web server. It is not a production server and does not replace the HTTP interpreter runtime.
